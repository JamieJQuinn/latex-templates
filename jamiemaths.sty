\usepackage{amsmath}
\usepackage{mathtools}
\usepackage[l2tabu, orthodox]{nag}
% Better derivatives
\usepackage{physics}
% show eqn no only when actually referenced
\mathtoolsset{showonlyrefs=true}
% better command definition
\usepackage{xparse}
% Better bold
\usepackage{bm}
% graphing
%\usepackage{tikz}
% hyperrefs
\usepackage[svgnames]{xcolor}
\colorlet{linkcolour}{DarkBlue}
\usepackage[pdfusetitle]{hyperref}
\hypersetup{colorlinks=true, linkcolor=linkcolour, citecolor=linkcolour, urlcolor=linkcolour,}

% Let's make vectors bold
\renewcommand\vec{\bm}
% let's define the lagrangian derivative
\NewDocumentCommand\Dt{mO{\vec{u}}}{\pdv{#1}{t} + (#2 \cdot \nabla) #1}
\newcommand{\ldv}[1]{\frac{D{#1}}{Dt}}
% nabla operators
\renewcommand\div{\text{div}\ }
\newcommand\Div{\text{Div}\ }
\renewcommand\grad{\text{grad}\ }
\newcommand\Grad{\text{Grad}\ }
\renewcommand\curl{\text{curl}\ }
\newcommand\Curl{\text{Curl}\ }

% Let's make a theorem
\newtheorem{theorem}{Theorem}
